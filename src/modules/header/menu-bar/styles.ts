import styled from "styled-components";
import { picoloLight } from "styles/fonts/fonts";

export const MenuBarC = styled.div`
  border-bottom: 2px solid ${({ theme }) => theme.colors.quinary};
`;

export const Ul = styled.ul`
  display: flex;
`;
